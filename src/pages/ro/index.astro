---
import { DEFAULT_LANGUAGE, type Language } from '../../lib/i18n';
import Seo from '../../components/Seo.astro';
import Layout from "../../layouts/Layout.astro";
import Card from "../../components/Card/index.astro";
import IntroCard from "../../components/IntroCard.astro";
import ContactsCard from "../../components/ContactsCard.astro";
import DonateCard from "../../components/DonateCard.astro";
import TimeZone from "../../components/TimeZoneCard.astro";
import AboutMe from "../../components/AboutMe.astro";
import Now from "../../components/Now.astro";
import DesignWorksCard from "../../components/DesignWorksCard.astro";
import { SITE } from "../../site-config";

const lang: Language = 'ro';
const isRO = true;
---

<Layout
  description="Portofoliu de securitate cibernetică axat pe ethical hacking, investigații digitale, răspuns la incidente și testare de securitate"
  title={`${SITE.author.fullName} - ${SITE.author.jobTitle}`}
  lang={lang}
>
  <Seo
    title={`${SITE.author.fullName} - ${SITE.author.jobTitle}`}
    description="Portofoliu de securitate cibernetică axat pe ethical hacking, investigații digitale, răspuns la incidente și testare de securitate"
    lang={lang}
    type="profile"
    keywords={["securitate cibernetică", "criminalistică digitală", "răspuns la incidente", "testare de penetrare", "evaluarea vulnerabilităților"]}
    canonical={`${SITE.site.url}ro/`}
  />

  <main
    class="text-white m-auto p-2 grid gap-2 max-w-6xl overflow-hidden relative w-full sm:p-4 sm:gap-2 md:grid-cols-2 md:gap-3 md:p-6 lg:h-[800px] lg:grid-rows-8 lg:grid-cols-4 lg:gap-4"
  >
    <IntroCard lang={lang} />
    <AboutMe lang={lang} />
    <ContactsCard lang={lang} />
    <TimeZone />
    <DesignWorksCard />
    <Now lang={lang} />
    <Card
      href="/ro/contact"
      colSpan="md:col-span-1"
      rowSpan="md:row-span-1"
      title="Contact"
    />
    <Card
      href="/ro/experience"
      colSpan="md:col-span-1"
      rowSpan="md:row-span-1"
      title="Experiență"
    />
    <Card
      href="/ro/blog"
      colSpan="md:col-span-1"
      rowSpan="md:row-span-1"
      title="Blog"
    />
    <DonateCard lang={lang} />

    <Card colSpan="md:col-span-1" rowSpan="md:row-span-1">
      <p class="text-xs">
        © 2025 · Creat cu ♥️ folosind <a
          class="text-red-500"
          href="https://astro.build/"
          target="_blank">Astro</a
        > de mine.
      </p>
    </Card>
  </main>
</Layout>

<script>
  import { stagger, animate, type AnimationSequence } from "motion";
  import { loaderAnimation } from "../../lib/constants";
  const cards = document.querySelectorAll(".card");

  const sequence = [
    loaderAnimation,
    [
      cards,
      { y: ["40%", "0%"], opacity: [0, 1] },
      {
        type: "spring",
        at: "-0.1",
        delay: stagger(0.2),
      },
    ],
  ] as AnimationSequence;

  animate(sequence);
</script>
