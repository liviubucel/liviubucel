---
import { DEFAULT_LANGUAGE, type Language } from '../lib/i18n';
import Seo from '../components/Seo.astro';
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card/index.astro";
import IntroCard from "../components/IntroCard.astro";
import ContactsCard from "../components/ContactsCard.astro";
import DonateCard from "../components/DonateCard.astro";
import TimeZone from "../components/TimeZoneCard.astro";
import AboutMe from "../components/AboutMe.astro";
import Now from "../components/Now.astro";
import DesignWorksCard from "../components/DesignWorksCard.astro";
import NewsletterSignup from "../components/NewsletterSignup.astro";
import { SITE } from "../site-config";
import { getPageSeo } from "../lib/sanity-queries";

export const prerender = false;

const lang: Language = 'en';
const isRO = false;
const newsletterStatus = Astro.url.searchParams.get('newsletter');

const seo = await getPageSeo("home");
const seoTitle = seo?.title || `${SITE.author.fullName} - ${SITE.author.jobTitle}`;
const seoDescription = seo?.description || "Cybersecurity portfolio focused on ethical hacking, digital investigation, incident response, security testing, and practical security projects";
const seoKeywords = seo?.keywords || ["cyber security", "digital forensics", "incident response", "penetration testing", "vulnerability assessment", "threat research"];
---

<Layout
  description={seoDescription}
  title={seoTitle}
  lang={lang}
>
  <Seo
    title={seoTitle}
    description={seoDescription}
    lang={lang}
    type="profile"
    keywords={seoKeywords}
    ogImage={seo?.ogImage}
    canonical={`${SITE.site.url}`}
  />

  <main
    class="text-white m-auto p-2 grid gap-2 max-w-6xl overflow-hidden relative w-full sm:p-4 sm:gap-2 md:grid-cols-2 md:gap-3 md:p-6 lg:h-[800px] lg:grid-rows-8 lg:grid-cols-4 lg:gap-4"
  >
    <IntroCard lang={lang} />
    <AboutMe lang={lang} />
    <ContactsCard lang={lang} />
    <TimeZone lang={lang} />
    <DesignWorksCard lang={lang} />
    <Now lang={lang} />
    <Card
      href="/contact"
      colSpan="md:col-span-1"
      rowSpan="md:row-span-1"
      title="Contact"
    />
    <Card
      href="/blog"
      colSpan="md:col-span-1"
      rowSpan="md:row-span-1"
      title="Blog"
    />
    <DonateCard lang={lang} />

    <Card colSpan="md:col-span-1" rowSpan="md:row-span-1">
      <p class="text-xs">
        © 2025 · Crafted with ♥️ using <a
          class="text-red-500"
          href="https://astro.build/"
          target="_blank">Astro</a
        > by Me.
      </p>
    </Card>
  </main>

  <NewsletterSignup initialStatus={newsletterStatus} lang={lang} />
</Layout>

<script>
  import { stagger, animate, type AnimationSequence } from "motion";
  import { loaderAnimation } from "../lib/constants";
  const cards = document.querySelectorAll(".card");

  const sequence = [
    loaderAnimation,
    [
      cards,
      { y: ["40%", "0%"], opacity: [0, 1] },
      {
        type: "spring",
        at: "-0.1",
        delay: stagger(0.2),
      },
    ],
  ] as AnimationSequence;

  animate(sequence);
</script>
